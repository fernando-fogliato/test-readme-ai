```markdown
## [Upcoming Release] - PR #18
### Added
- Added a new REST API resource for managing `Address` entities. This introduces a new `/api/addresses` endpoint, providing full CRUD (Create, Read, Update, Delete) operations for address records.
```