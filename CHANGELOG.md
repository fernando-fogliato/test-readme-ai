```markdown
## YYYY-MM-DD - PR #20
### Added
-   **Group Management API:** Introduced a complete set of RESTful API endpoints for managing `Group` resources. This includes functionality to create, retrieve (individual and all), update, and delete `Group` entities.
    -   A new `Group` entity, `GroupRepository`, `GroupService`, and `GroupController` have been added to support these operations.
```